# Xcode — the project is generated from project.yml (XcodeGen); do not commit it
*.xcodeproj/
*.xcworkspace/xcuserdata/
xcuserdata/
DerivedData/
*.xcresult

# SPM
.build/
.swiftpm/
Package.resolved

# macOS
.DS_Store

# Env / secrets
*.env
*.p8
*.p12
*.mobileprovision

# Agent worktrees + local claude config
.claude/worktrees/

# Verification screenshots (drift gate)
.shots/

# local dev credentials (never commit)
.mrt-backend-token*
